# Model catalog for localtagger.
#
# Adding a new model is a config change, not a code change: append an entry here
# (and, if it needs a new inference path, a backend under app/backends/). The
# `family` field selects which backend runs the model:
#   wd14      -> imgutils.tagging.get_wd14_tags   (ONNX, auto-downloaded)
#   pixai     -> imgutils.tagging.get_pixai_tags  (ONNX, auto-downloaded)
#   camie     -> imgutils.tagging.get_camie_tags  (ONNX, auto-downloaded)
#   animetimm -> timm + safetensors               (gated, needs HF_TOKEN)
#   wd14_st   -> timm + safetensors, WD14 preprocessing (ungated; for WD-family
#                taggers published without an ONNX export)
#
# `group` is the display heading the frontend groups models under (e.g. all
# WD-family taggers sit beneath "WD Tagger v3"). `label` is the short
# architecture name shown inside that group; `description` is the one-liner.
# These three drive a two-level picker:  group -> label (description).
#
# `default: true` marks a model as enabled out-of-the-box. The ENABLED_MODELS
# env var (comma-separated ids) overrides this set when provided.
#
# Per-tag `category` for animetimm: 0=general, 4=character, 9=rating.

models:
  # --- WD Tagger v3 ----------------------------------------------------------
  - id: wd-eva02-large-v3
    group: wdtagger
    label: EVA02
    description: the best of the original wd taggers
    family: wd14
    repo: SmilingWolf/wd-eva02-large-tagger-v3
    model_name: EVA02_Large
    gated: false
    recommended: true
    default: true
    default_threshold: 0.35
    default_character_threshold: 0.85

  - id: wd-swinv2-v3
    group: wdtagger
    label: SwinV2
    description: fast and balanced predecessor to eva02
    family: wd14
    repo: SmilingWolf/wd-swinv2-tagger-v3
    model_name: SwinV2_v3
    gated: false
    recommended: false
    default: true
    default_threshold: 0.35
    default_character_threshold: 0.85

  - id: wd-eva02-canary-2026
    group: wdtagger
    label: EVA02 Canary
    description: wd eva02 updated to 2026
    family: wd14_st
    repo: ashen-sensored/wd-eva02-tagger-2026-canary
    model_name: ""
    gated: false
    recommended: true
    default: true
    default_threshold: 0.55
    default_character_threshold: 0.8

  - id: pixai-v0.9
    group: wdtagger
    label: Pixai v0.9
    description: eva02 trained further but inconsistent
    family: pixai
    repo: pixai-labs/pixai-tagger-v0.9
    model_name: v0.9
    gated: false
    recommended: false
    default: true
    default_threshold: 0.35
    default_character_threshold: 0.85

  # --- Animetimm dbv4 --------------------------------------------------------
  - id: animetimm-mobilenetv4
    group: animetimm
    label: MobileNetV4
    description: mini instant tagger
    family: animetimm
    repo: Mooshie/mobilenetv4_conv_aa_large.dbv4-full
    model_name: ""
    gated: false
    recommended: false
    default: true
    default_threshold: 0.35
    default_character_threshold: 0.85

  - id: animetimm-swinv2-base
    group: animetimm
    label: SwinV2
    description: fast and good
    family: animetimm
    repo: Mooshie/swinv2_base_window8_256.dbv4-full
    model_name: ""
    gated: false
    recommended: false
    default: true
    default_threshold: 0.35
    default_character_threshold: 0.85

  - id: animetimm-caformer-b36
    group: animetimm
    label: CAFormer
    description: balanced and recommended by animetimm
    family: animetimm
    repo: Mooshie/caformer_b36.dbv4-full
    model_name: ""
    gated: false
    recommended: false
    default: true
    default_threshold: 0.35
    default_character_threshold: 0.85

  - id: animetimm-eva02-large
    group: animetimm
    label: EVA02
    description: pretty big but amazing
    family: animetimm
    repo: Mooshie/eva02_large_patch14_448.dbv4-full
    model_name: ""
    gated: false
    recommended: false
    default: true
    default_threshold: 0.35
    default_character_threshold: 0.85

  - id: animetimm-convnextv2-huge
    group: animetimm
    label: ConvNeXt
    description: huge but deadly accurate
    family: animetimm
    repo: Mooshie/convnextv2_huge.dbv4-full
    model_name: ""
    gated: false
    recommended: true
    default: true
    default_threshold: 0.35
    default_character_threshold: 0.85

  # --- Camie v2 --------------------------------------------------------------
  - id: camie-v2
    group: camie
    label: Camie
    description: different feel and more inconsistent
    family: camie
    repo: Camais03/camie-tagger-v2
    model_name: initial
    gated: false
    recommended: false
    default: true
    default_threshold: 0.35
    default_character_threshold: 0.85
